---
description: 完成代码修改后自动停止并重启开发服务器
alwaysApply: true
---

# 开发服务器重启

每次完成对项目代码的修改后，请自动执行：

1. **停止**当前正在运行的开发进程（如 `npm run dev` / `next dev` 占用的端口，默认 3000）。
2. **重新启动**开发服务器（在项目根目录执行 `npm run dev`，可后台运行）。

这样修改能立即生效，用户无需手动重启。若当前没有检测到开发服务器在运行，直接启动即可。
